{
  "name": "hakkutsu-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter ./extension dev",
    "dev:extension": "pnpm --filter ./extension dev",
    "build:extension": "pnpm --filter ./extension build"
  }
}
